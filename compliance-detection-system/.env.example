# Google AI Studio API Configuration (Recommended - Simple API Key)
GOOGLE_API_KEY=your-google-ai-studio-api-key-here
# Alternative name for the same key
# GEMINI_API_KEY=your-google-ai-studio-api-key-here

# Google Cloud Configuration (Alternative - More Complex Setup)
# GOOGLE_CLOUD_PROJECT=your-project-id-here
GEMINI_LOCATION=us-central1
GEMINI_MODEL=gemini-2.0-flash
GEMINI_TEMPERATURE=0.2
GEMINI_MAX_TOKENS=2048

# Authentication for Google Cloud (only needed if using Google Cloud instead of API key)
# Method 1: Service Account Key File
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your-service-account-key.json

# Method 2: Application Default Credentials (after running gcloud auth)
# Will automatically use your gcloud credentials

# Other settings
DATA_DIR=./data
ARTIFACT_DIR=./artifacts
LOG_LEVEL=INFO
DATABASE_URL=sqlite:///./registry.db
SEMGREP_CONFIG=./data/rules/semgrep.yml
TREESITTER_LANGUAGES=python,typescript,javascript

# Runtime Configuration  
PLAYWRIGHT_HEADLESS=true
BROWSER_TIMEOUT=30000

# Output Configuration
CSV_OUTPUT_PATH=./artifacts/final.csv
HTML_REPORT_PATH=./artifacts/report.html
